/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'k.kakaocdn.net', 'avatars.githubusercontent.com'],
  },
}

module.exports = nextConfig
